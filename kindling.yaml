name: bootstrap
description: Our one-line dev environment bootstrapper.
repo: https://github.com/buildingenergy/bootstrap
setup:
    system_requirements:
    codebases:
        - "bootstrap": "git@github.com:buildingenergy/bootstrap.git"
    primary_codebase: bootstrap

start:
    bootstrap:

stop:
    bootstrap:

destroy:
    bootstrap:

test:
    
